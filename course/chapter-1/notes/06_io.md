# 06 — Streams and formatting

## Goals
- Use `std::cout` and `std::cin`.
- Format output with `iomanip`.

## Topics
- `std::cout`, `std::cerr`
- `std::getline`
- `std::fixed`, `std::setprecision`

## Exercises
- Read a line and print its length.
- Format a double to 3 decimal places.
